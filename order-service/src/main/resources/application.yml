app:
  api-gateway-url: https://localhost:8080
server:
  port: 8080
spring:
  application:
    name: order-service
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/eats
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  jackson:
    time-zone: Europe/Moscow
